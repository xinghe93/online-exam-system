# Java Web 实验报告 —— 在线考试系统

---

## 一、功能设计描述 —— 试题管理模块

### 1.1 功能概述

试题管理是在线考试系统的核心功能之一，面向管理员角色提供完整的试题（题目）CRUD 操作，包括：
- **添加试题**：填写题目描述、四个选项（A/B/C/D）并指定正确答案
- **浏览试题列表**：以表格形式展示所有试题，支持查看详情
- **编辑试题**：修改已有试题的题干、选项或正确答案
- **删除试题**：移除指定试题

### 1.2 设计过程

#### 1.2.1 需求分析

试题管理的设计从三个核心问题出发：

**目标用户是谁？**
管理员——需要一套简洁高效的工具来维护题库，无需关心底层数据库操作。

**需要解决什么问题？**
- 人工维护 SQL 脚本管理试题效率低、易出错
- 缺少对试题内容的集中查看和检索手段
- 编辑和删除现有试题需要直接操作数据库，风险高

**功能边界如何划定？**
- 试题是单选题，一道题有且仅有一个正确答案
- 只涉及题目本身的管理，不涉及试卷组卷逻辑
- 操作粒度是单题增删改，不支持批量导入导出（留给后续扩展）

基于上述分析，确定了四个原子操作：**添加**(Add)、**浏览列表**(List)、**编辑**(Edit)、**删除**(Delete)，即完整的 CRUD。

#### 1.2.2 界面与交互设计

试题管理模块共设计三个 JSP 页面，每个页面对应一个操作场景：

**1. 试题列表页 (questionList.jsp)**
- 进入试题管理的默认页面，顶部提供"添加试题"按钮和导航链接
- 主体为 HTML `<table>` 表格，每一行显示一道题的题干摘要和选项预览
- 每行末尾放置"编辑"和"删除"操作按钮——编辑跳转到编辑页面，删除通过点击触发确认弹窗（`confirm()`）后再提交请求
- 表格底部显示总题数和当前页码（预留分页扩展点）
- 操作成功/失败的状态提示通过 URL 参数（`?success=add`、`?error=deleteFailed`）在页面顶部渲染

**2. 添加试题页 (questionAdd.jsp)**

- 表单包含：一个多行文本框（`<textarea>`）输入题干、四个单行输入框输入 A/B/C/D 选项、一组 radio button 选择正确答案
- 前端 JavaScript 做即时校验：题干和选项不能为空、正确答案必须选择
- 底部"提交"和"取消"按钮，取消则返回列表页
- 提交后 POST 到 `/questionManage?action=add`，由 Servlet 处理后端校验

**3. 编辑试题页 (questionEdit.jsp)**
- 与添加页布局一致，但表单被预先回填当前试题的题干、选项和答案
- 进入编辑时通过 `GET /questionManage?action=edit&questionId=X` 由 Servlet 将查询到的 Question 对象存入 request 域，JSP 用 `<%= question.getTitle() %>` 回填
- 提交后 POST 到 `/questionManage?action=update`，后端执行 UPDATE 操作
- "取消"返回列表页

**页面间导航关系**：
```
questionList.jsp  ←──→  questionAdd.jsp
     ↕
 questionEdit.jsp
```
列表页作为"主页"，添加和编辑页完成后均重定向回列表页。

#### 1.2.3 类的设计

试题管理模块涉及三个核心类，分工明确：

**Question 实体类** (`com.xinghe.onlineexam.entity.Question`)
- JavaBean 规范：私有字段 + 无参构造 + getter/setter
- 字段与数据库 `questions` 表列一一对应：`questionId`、`title`、`optionA`~`optionD`、`answer`
- 纯数据容器，不包含业务方法

**QuestionDao 接口** (`com.xinghe.onlineexam.dao.QuestionDao`)

```java
public interface QuestionDao {
    List<Question> findAllQuestions();           // 查询所有试题
    Question findQuestionById(int questionId);   // 根据 ID 查询单条
    int addQuestion(Question question);           // 添加试题，返回影响行数
    int updateQuestion(Question question);        // 更新试题
    int deleteQuestion(int questionId);           // 删除试题
}
```
每个方法对应一个 CRUD 操作，返回类型统一——`List<Question>` 查集合、`Question` 查单条、`int` 返回值表示数据库受影响行数用于判断成败。

**QuestionDaoImpl 实现类** (`com.xinghe.onlineexam.dao.impl.QuestionDaoImpl`)

- 每个方法内部遵循相同的 JDBC 模板：`getConnection()` → `prepareStatement()` → `executeQuery()/executeUpdate()` → 封装结果 → `close()` 释放资源
- 查询方法通过 `mapResultSetToQuestion(ResultSet rs)` 私有方法将 ResultSet 的行数据映射为 Question 对象，消除重复的取值代码：
  ```java
  private Question mapResultSetToQuestion(ResultSet rs) throws SQLException {
      Question q = new Question();
      q.setQuestionId(rs.getInt("questionId"));
      q.setTitle(rs.getString("title"));
      // ... 选项映射
      q.setAnswer(rs.getString("answer").charAt(0));
      return q;
  }
  ```
- 所有 SQL 均使用 `PreparedStatement` 的 `?` 占位符传参，杜绝 SQL 注入风险
- 方法内使用 `try-catch-finally` 确保数据库连接和语句对象在异常时也能被关闭

**QuestionServlet 控制类** (`com.xinghe.onlineexam.servlet.QuestionServlet`)

- `@WebServlet("/questionManage")` 注解映射 URL，无需 web.xml 配置
- `doGet()`：处理 GET 请求，根据 `action` 参数分发到三个分支——
  - `action=list`：查询全部试题，转发到列表页
  - `action=add`：转发到添加页
  - `action=edit`：查询单条试题（`findQuestionById`），将结果存入 request，转发到编辑页
- `doPost()`：处理 POST 请求（表单提交），根据 `action` 参数分发——
  - `action=add`：校验参数 → 封装实体 → DAO 添加 → 重定向（成功到列表、失败回添加页）
  - `action=update`：校验参数 → DAO 更新 → 重定向到列表页
  - `action=delete`：根据 `questionId` 执行 DAO 删除 → 重定向到列表页

**设计要点**：通过 `action` 参数在同一 Servlet 内聚合多个操作，避免了为每个操作创建独立 Servlet 的类爆炸问题。`doGet` 负责"读取页面路由"，`doPost` 负责"写入操作"，天然符合 HTTP 方法的语义约定。

#### 1.2.4 权限控制设计

试题管理的所有操作仅对管理员开放，权限控制的实现贯穿前后端：

**前端防护**：

- 只有角色为 admin 的用户才能在导航栏看到"试题管理"入口
- 非管理员访问 `/questionManage` 直接跳转首页，入口不在菜单中展示

**后端防护（核心防线）**：

```java
// QuestionServlet 中每个请求处理前的权限检查
User user = (User) req.getSession().getAttribute("user");
if (user == null || !"admin".equals(user.getRole())) {
    resp.sendRedirect(req.getContextPath() + "/content.jsp?error=denied");
    return;
}
```
- `doGet()` 和 `doPost()` 的 **最开头** 都执行此检查
- 双重判断：Session 中 `user` 对象是否存在（是否登录）+ `role` 是否为 `"admin"`
- 不满足条件则重定向到主页并附带 `error=denied` 参数，不做任何数据库操作

**设计考量**：权限必须在 Controller 层把关，不能依赖前端的隐藏/显示。这是因为恶意请求可以直接绕过浏览器界面构造 HTTP 请求，只有服务端校验才是真正可靠的防线。

#### 1.2.5 数据校验设计

校验分为前端即时校验和后端最终校验两层：

**前端校验（提升用户体验）**：

- 在表单 `submit` 事件的 JavaScript 监听函数中检查必填项
- 题干和选项为空时弹出提示并阻止提交
- 正确答案未选择时提示选择
- 前端校验主要为用户提供即时反馈，避免无效提交带来的等待

**后端校验（安全底线）**：

```java
// 以添加试题为例的核心校验逻辑
String title = req.getParameter("title");
String optionA = req.getParameter("optionA");
// ... 选项 B/C/D
String answer = req.getParameter("answer");

if (title == null || title.trim().isEmpty() ||
    optionA == null || optionA.trim().isEmpty() ||
    /* ... 其他选项判空 */) {
    resp.sendRedirect("questionManage?action=add&error=emptyFields");
    return;
}

String answerUpper = answer.toUpperCase();
if (!"A".equals(answerUpper) && !"B".equals(answerUpper) &&
    !"C".equals(answerUpper) && !"D".equals(answerUpper)) {
    resp.sendRedirect("questionManage?action=add&error=invalidAnswer");
    return;
}
```
- 空值检查：所有字段依次判空，`trim()` 去除空白后检测
- 答案范围检查：限制在 A~D 四个合法值内，防止传入非法字符
- 校验失败时重定向回表单页面，附带具体的错误码参数，JSP 根据参数显示不同错误提示

**设计要点**：后端校验必须假设所有输入都是不可信的。即使前端已经校验过，后端仍需逐项检查，这是安全编程的基本要求。校验失败返回错误码而非抛出异常，保证用户体验的连贯性。

#### 1.2.6 模块架构

试题管理采用经典 MVC 分层结构：

```
┌─────────────────────────────────────────────────┐
│                   视图层 (View)                   │
│  questionAdd.jsp   questionEdit.jsp   questionList.jsp │
└───────────────────────┬─────────────────────────┘
                        │ HTTP 请求/转发
┌───────────────────────▼─────────────────────────┐
│                 控制层 (Controller)               │
│              QuestionServlet.java                 │
│           @WebServlet("/questionManage")          │
└───────────────────────┬─────────────────────────┘
                        │ 方法调用
┌───────────────────────▼─────────────────────────┐
│                 模型层 (Model)                    │
│  ┌──────────────────┐  ┌──────────────────────┐ │
│  │ QuestionDao接口   │  │   Question实体类      │ │
│  │ QuestionDaoImpl   │  │   (JavaBean)         │ │
│  └──────────────────┘  └──────────────────────┘ │
└───────────────────────┬─────────────────────────┘
                        │ JDBC
┌───────────────────────▼─────────────────────────┐
│                 数据库 (MySQL)                    │
│                questions 表                      │
└─────────────────────────────────────────────────┘
```

**各层职责**：

- **视图层**（3 个 JSP 文件）：负责数据呈现和用户交互，不包含业务逻辑。通过 EL 表达式、Scriptlet 从 request 域中读取数据渲染页面，通过表单和超链接发起请求
- **控制层**（QuestionServlet）：担任"交通警察"角色——接收请求、解析参数、检查权限、调用 DAO、决定路由。不直接操作数据库，也不生成 HTML
- **模型层**（Question + QuestionDao/Impl）：Question 是数据载体，DAO 是数据访问契约，Impl 是 JDBC 具体实现。变化集中在模型层：数据库变更只需修改 Impl，字段变更只需修改 Question
- **数据库**（MySQL + questions 表）：持久化存储层

#### 1.2.7 数据库表设计

```sql
CREATE TABLE questions (
    questionId INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(500) NOT NULL  COMMENT '题目描述',
    optionA VARCHAR(200) NOT NULL COMMENT '选项A',
    optionB VARCHAR(200) NOT NULL COMMENT '选项B',
    optionC VARCHAR(200) NOT NULL COMMENT '选项C',
    optionD VARCHAR(200) NOT NULL COMMENT '选项D',
    answer CHAR(1) NOT NULL COMMENT '正确答案 A/B/C/D'
);
```

**设计考量**：

- **单选题约束**：`answer` 字段类型为 `CHAR(1)`，仅存储 A/B/C/D 四个字符，从数据库层面约束正确答案的格式
- **长度控制**：`title` 的 VARCHAR(500) 覆盖绝大多数试题题干长度；选项 VARCHAR(200) 适中，兼顾存储效率和内容容量
- **自增主键**：`questionId` 使用 `INT AUTO_INCREMENT`，由数据库自动分配 ID，避免应用层生成主键的冲突问题
- **NOT NULL 约束**：所有业务字段均不允许为 NULL，将空值校验下沉到数据库层，作为后端校验的最后一道防线
- **无外键设计**：试题表独立存在，不与考试记录等表建立外键约束。这是因为试题可能在引用后仍需要独立修改（如纠错），外键约束会限制灵活性，将数据完整性交由应用层逻辑保证

#### 1.2.8 请求处理流程

以 **添加试题** 为例，完整的请求-响应流程如下：

```
用户点击"添加试题"
       │
       ▼
浏览器 GET /questionManage?action=add
       │
       ▼
QuestionServlet.doGet() 拦截请求
       │
       ├── 检查 Session 中是否有 user 对象 → 未登录则重定向到登录页
       ├── 检查 user.role 是否为 "admin" → 非管理员则返回主页
       └── 通过后：转发到 /pages/questionAdd.jsp
       │
       ▼
questionAdd.jsp 渲染添加表单
用户填写并提交
       │
       ▼
浏览器 POST /questionManage (action=add)
       │
       ▼
EncodingFilter.doFilter() 统一设置 UTF-8 编码
       │
       ▼
QuestionServlet.doPost() 处理请求
       │
       ├── 再次检查登录状态和角色权限
       ├── 提取参数：title, optionA~D, answer
       ├── 参数校验：非空检查 + 答案范围检查（A~D）
       ├── 封装 Question 实体（JavaBean）
       ├── 调用 questionDao.addQuestion(question) → JDBC INSERT
       └── 成功：重定向到列表页（?success=add）
           失败：重定向回添加页（?error=addFailed）
       │
       ▼
浏览器 GET /questionManage?action=list
       │
       ▼
QuestionServlet.doGet()
       ├── 调用 questionDao.findAllQuestions() → JDBC SELECT
       ├── 将结果存入 request.setAttribute("questions", list)
       └── 转发到 /pages/questionList.jsp 渲染表格
```

**编辑试题** 流程类似（`action=edit` → 根据 questionId 查询并回填表单 → 提交 `action=update` → UPDATE 操作），**删除试题** 流程（`action=delete` → DELETE 操作 → 重定向回列表页）。

#### 1.2.9 流程图

```
                   ┌───────────────┐
                   │  请求到达       │
                   └───────┬───────┘
                           │
                   ┌───────▼───────┐
                   │ EncodingFilter │  ← Filter 统一编码
                   │ set UTF-8     │
                   └───────┬───────┘
                           │
                   ┌───────▼───────┐
                   │  已登录？       │────否──→ 重定向登录页
                   └───────┬───────┘
                           │ 是
                   ┌───────▼───────┐
                   │  是管理员？     │────否──→ 重定向主页(error=denied)
                   └───────┬───────┘
                           │ 是
                     ┌─────┴─────┐
                     │           │
               ┌─────▼────┐ ┌───▼────┐
               │  GET请求  │ │POST请求│
               └─────┬────┘ └───┬────┘
                     │           │
              ┌──────┴──────┐   │
              │ action=?    │   │
        ┌─────┼──┬──┬──┬───┘   │
        │     │  │  │  │       │
   ┌────▼┐ ┌─▼┐┌▼─┐┌▼─┐ │  ┌──┴──────────┐
   │list │ │e-││a-││de│ │  │ action=?    │
   │查   │ │dit││dd││le│ │  │ add/update/ │
   │询   │ │查 ││转││te│ │  │ delete      │
   │所   │ │询 ││发││删│ │  └──────┬──────┘
   │有   │ │单││添││除│ │         │
   │试题 │ │条││加││事│ │    ┌─────┴──────┐
   └──┬──┘ │ │页││件│ │    │ 参数校验     │
      │    └─┘└─┘└─┘ │    │ 封装实体     │
      │               │    │ DAO 调用     │
   ┌──▼──────────┐    │    └──────┬──────┘
   │ 转发 JSP    │    │           │
   │ 渲染视图    │    │    ┌──────▼──────┐
   └─────────────┘    │    │ 成功/失败  │
                      │    │ 重定向+参数│
                      │    └─────────────┘
                      │
              ┌───────▼─────────┐
              │ Listener 隐式工作 │  ← Listener 监控 Session
              │ UserSessionListener │
              │ sessionCreated/   │
              │ sessionDestroyed  │
              └──────────────────┘
```

### 1.3 Filter 在功能中的应用

**EncodingFilter** (`com.xinghe.onlineexam.filter.EncodingFilter`)

- **注解配置**：`@WebFilter("/*")` 拦截所有请求
- **核心作用**：统一设置请求和响应的字符编码为 UTF-8，解决中文字符乱码问题
- **在试题管理中的应用**：管理员添加/编辑试题时提交的题目描述和选项内容可能包含中文，Filter 确保 `request.getParameter()` 读取到的参数不会出现乱码

```java
// 核心代码
public void doFilter(ServletRequest request, ServletResponse response, FilterChain chain)
        throws IOException, ServletException {
    request.setCharacterEncoding(encoding);    // UTF-8
    response.setCharacterEncoding(encoding);
    response.setContentType("text/html;charset=" + encoding);
    chain.doFilter(request, response);
}
```

**设计要点**：

- 通过 Filter 集中处理编码，每个 Servlet 无需再单独设置编码（QuestionServlet.doPost 中虽然有 `req.setCharacterEncoding("UTF-8")`，但只要 Filter 正常运行即可删除该行）
- `init()` 方法支持从 `web.xml` 初始化参数读取编码配置，保障灵活性
- Filter 的生命周期（init → doFilter → destroy）由 Web 容器管理

### 1.4 Listener 在功能中的应用

**UserSessionListener** (`com.xinghe.onlineexam.listener.UserSessionListener`)

- **注解配置**：`@WebListener` 监听 HttpSession 的创建与销毁事件
- **核心作用**：在试题管理功能中虽然不直接参与业务逻辑，但其维护的在线用户列表为管理员的操作审计提供了会话上下文

```java
// 核心机制
@WebListener
public class UserSessionListener implements HttpSessionListener {
    private static final ConcurrentHashMap<String, User> onlineUsers = new ConcurrentHashMap<>();

    @Override
    public void sessionCreated(HttpSessionEvent se) {
        System.out.println("[Session] 会话创建: " + se.getSession().getId());
    }

    @Override
    public void sessionDestroyed(HttpSessionEvent se) {
        User user = (User) session.getAttribute("user");
        if (user != null) {
            onlineUsers.remove(session.getId());
        }
    }

    public static void addUser(HttpSession session, User user) {
        onlineUsers.put(session.getId(), user);
    }
    // ...
}
```

**设计要点**：
- 使用 `ConcurrentHashMap` 保证多线程环境下的线程安全
- 用户登录时（LoginServlet）显式调用 `UserSessionListener.addUser()`
- 会话超时或主动注销时通过 `sessionDestroyed` 事件自动移除在线记录
- 前端通过 `OnlineUsersServlet` 周期性（10s）拉取在线人数显示在顶栏

### 1.5 开发注意事项

1. **权限控制**：试题管理涉及敏感操作，必须进行双重权限校验——既检查 Session 是否过期，又检查用户角色是否为 admin，且 **每次请求**（doGet 和 doPost 均需）都要检查，不可依赖前端限制
2. **参数校验**：后端必须对用户输入做空值和合法性校验（如答案必须为 A~D），防止恶意构造请求绕过前端限制
3. **SQL 注入防范**：始终使用 PreparedStatement 参数化查询，禁止拼接 SQL 字符串
4. **编码一致性**：JSP 页面声明 `contentType="text/html;charset=UTF-8"`、Filter 设置 UTF-8、数据库连接 URL 指定 `characterEncoding=UTF-8`，三者必须一致
5. **事务管理**：当前 DAO 层使用自动提交模式，在涉及多表操作（如考试提交需同时保存答案和更新考试记录）时，应使用事务确保原子性
6. **错误反馈**：操作失败时应通过 URL 参数（如 `?error=xxx` 或 `?success=xxx`）向前端传递明确的状态信息，便于用户了解操作结果

---

## 二、完整代码提交

实验项目全部代码已按包结构整理，需要将项目目录打包为压缩包。

### 包结构

```
src/com/xinghe/onlineexam/
├── dao/
│   ├── UserDao.java                    # 用户 DAO 接口
│   ├── QuestionDao.java                # 试题 DAO 接口
│   ├── ExamDao.java                    # 考试 DAO 接口
│   └── impl/
│       ├── UserDaoImpl.java            # 用户 DAO 实现
│       ├── QuestionDaoImpl.java        # 试题 DAO 实现
│       └── ExamDaoImpl.java            # 考试 DAO 实现
├── entity/
│   ├── User.java                       # 用户 JavaBean
│   ├── Question.java                   # 试题 JavaBean
│   ├── ExamRecord.java                 # 考试记录 JavaBean
│   └── ExamAnswer.java                 # 考试答案 JavaBean
├── servlet/
│   ├── LoginServlet.java               # 登录处理 /login
│   ├── RegisterServlet.java            # 注册处理 /register
│   ├── QuestionServlet.java            # 试题管理 /questionManage
│   ├── ExamServlet.java                # 考试处理 /exam
│   ├── UserServlet.java                # 用户管理 /userManage
│   └── OnlineUsersServlet.java         # 在线用户查询 /onlineUsers
├── filter/
│   └── EncodingFilter.java             # 编码过滤器
├── listener/
│   └── UserSessionListener.java        # Session 监听器
└── util/
    └── JDBCUtil.java                   # 数据库连接工具类

web/
├── WEB-INF/
│   └── web.xml                         # Web 配置文件
├── userlogin.jsp                       # 登录页面
├── register.jsp                        # 注册页面
├── content.jsp                         # 主页面（仪表盘）
├── fail.html                           # 登录失败页面
└── pages/
    ├── questionAdd.jsp                 # 添加试题
    ├── questionEdit.jsp                # 编辑试题
    ├── questionList.jsp                # 试题列表
    ├── examStart.jsp                   # 开始考试
    ├── examPage.jsp                    # 答题页面
    ├── examResult.jsp                  # 考试成绩
    ├── examHistory.jsp                 # 考试历史
    ├── userInfo.jsp                    # 个人信息
    ├── userList.jsp                    # 用户列表（管理员）
    └── userScore.jsp                   # 用户成绩（管理员）

src/
└── db.properties                       # 数据库配置文件

lib/
├── mysql-connector-j-8.0.33.jar       # MySQL 驱动
├── jbcrypt-0.4.jar                     # 密码加密库
└── servlet-api.jar                     # Servlet API
```

### 打包命令

```bash
# 项目根目录下执行
tar -czvf 学号_姓名_实验一代码提交.tar.gz \
    src/ web/ lib/ \
    OnlineExamSystem.iml \
    .gitignore
```

> **注意**：压缩包需包含 `lib/` 目录下的所有 JAR 包（mysql-connector-j-8.0.33.jar、jbcrypt-0.4.jar、servlet-api.jar），确保项目可直接在 IDEA + Tomcat 环境中导入运行，同时附上 `db.properties` 配置文件以便修改数据库连接参数。

---

## 三、调试错误记录

### 错误 1：javax.servlet 包名在 Tomcat 10 下编译失败

**错误现象**：

```
The import javax.servlet cannot be resolved
javax.servlet.annotation.WebServlet 无法识别
```

**原因分析**：

Tomcat 10 将 Servlet API 从包 `javax.servlet` 迁移到了 `jakarta.servlet`。项目最初在写 Servlet 时使用了 `import javax.servlet.*`，但当前配置的 Tomcat 版本为 10.x，对应的 servlet-api.jar 已经不再提供 `javax.servlet` 包下的类。

**改正过程**：

1. 确认项目依赖的 Tomcat 版本。通过检查 `web.xml` 中的命名空间发现使用的是 Servlet 4.0 规范（`xmlns.jcp.org`），但实际运行时容器是 Tomcat 10。
2. 将所有 Servlet、Filter、Listener 和相关工具类中的 `import javax.servlet.*` 全部替换为 `import jakarta.servlet.*`。
3. 具体涉及文件：
   - `LoginServlet.java`、`RegisterServlet.java`、`QuestionServlet.java`、`ExamServlet.java`、`UserServlet.java`、`OnlineUsersServlet.java`
   - `EncodingFilter.java`
   - `UserSessionListener.java`
4. 同时确认项目中使用的 servlet-api.jar 也更新为对应版本（或直接使用 Tomcat 10 自带的 API）。
5. 修改后重新编译部署，Servlet 映射正常生效。

---

### 错误 2：MySQL 连接时区错误导致数据库连接失败

**错误现象**：

```
java.sql.SQLException: The server time zone value '�й���ʱ��' is unrecognized
or represents more than one time zone.
```

**原因分析**：

MySQL 8.0 及以上版本对时区要求严格。`db.properties` 中的 JDBC URL 没有指定 `serverTimezone` 参数，而操作系统时区被识别为中文本地化名称（"中国标准时间"），驱动程序无法识别该非标准的时区名称。

**改正过程**：

1. 在 `db.properties` 的 JDBC URL 中添加时区参数：

```properties
# 修改前
jdbc.url=jdbc:mysql://localhost:3306/online_exam

# 修改后
jdbc.url=jdbc:mysql://localhost:3306/online_exam?serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
```

2. 增加的参数说明：
   - `serverTimezone=Asia/Shanghai`：明确指定时区为东八区
   - `useSSL=false`：本地开发环境关闭 SSL 加密，避免 SSL 握手警告
   - `allowPublicKeyRetrieval=true`：允许从服务器获取公钥，解决部分 MySQL 8.0 驱动的连接问题
3. 修改后重启 Tomcat，数据库连接恢复正常。

---

### 错误 3：BCrypt 密码验证始终返回 false

**错误现象**：

```java
// 登录校验代码
public boolean validateLogin(String userName, String password) {
    User user = findUserByName(userName);
    if (user != null) {
        return BCrypt.checkpw(password, user.getPassword());
    }
    return false;
}
```

注册时使用 `BCrypt.hashpw(password, BCrypt.gensalt())` 加密后插入数据库，但登录时 `BCrypt.checkpw()` 始终返回 `false`，导致用户无法登录。

**原因分析**：

检查后发现有两个问题：
1. 数据库 `password` 字段的长度设置为 VARCHAR(45)，而 BCrypt 生成的密文长度为 60 个字符，导致密码被截断存储
2. `UserDaoImpl.findUserByName()` 的 SQL 查询中，`SELECT` 字段列没有包含 `password` 列（早期版本使用 `SELECT *` 排除 password 列以提高安全性，但忽略了登录验证需要 password 字段）

**改正过程**：

1. 修改数据库表结构，将 `password` 字段长度调整为 VARCHAR(60)：

```sql
ALTER TABLE users MODIFY password VARCHAR(60);
```

2. 修正 `UserDaoImpl` 中的 SQL 查询语句，确保查询结果包含 `password` 字段：

```java
// 修正前
String sql = "SELECT userId, userName, sex, email, role FROM users WHERE userName = ?";

// 修正后（使用 mapResultSetToUser 方法）
String sql = "SELECT userId, userName, password, sex, email, role FROM users WHERE userName = ?";
```

3. 同时检查了 `findUserByEmail()` 和 `findUserById()` 方法，确保所有查询都返回 `password` 字段。
4. 删除已有测试用户数据，重新注册后登录成功。

---

### 错误 4：JSP 页面 EL 表达式无法解析

**错误现象**：

在 JSP 页面中形如 `${pageContext.request.contextPath}` 的 EL 表达式未被解析，在页面上原样显示为 `${...}` 文本。

**原因分析**：

JSP 2.0 及以上版本默认启用 EL 表达式解析，但如果 `web.xml` 中声明了 `<web-app version="2.4">` 或更早版本的 Schema，或通过 `isELIgnored` 指令显式禁用了 EL，则不会解析。本项目中 `web.xml` 使用的是 Servlet 4.0 Schema（对应 JSP 2.3），理论上应默认启用 EL。

进一步排查发现某些 JSP 页面头部缺少 `isELIgnored="false"`，且在不同 Servlet 容器间的行为存在差异。

**改正过程**：

1. 在 `web.xml` 中全局启用 EL 表达式（确保容器版本一致性）：

```xml
<jsp-config>
    <jsp-property-group>
        <url-pattern>*.jsp</url-pattern>
        <el-ignored>false</el-ignored>
    </jsp-property-group>
</jsp-config>
```

2. 在部分关键 JSP 页面头部明确声明：

```jsp
<%@ page isELIgnored="false" %>
```

3. 对于不适用 EL 表达式的页面，改用 `<%=request.getContextPath()%>` 的 Scriptlet 方式获取上下文路径（questionAdd.jsp、questionEdit.jsp 等使用了这种方法作为保险）。

---

## 四、实验总结

### 4.1 收获与体会

通过本次实验，我完整经历了一个基于 JSP + Servlet + JavaBean 的 Web 应用从设计到实现的全过程，获得了以下重要认识：

**1. MVC 分层思想的实践价值**

在线考试系统严格遵循了 MVC 架构：
- **Model（JavaBean + DAO）**：`User`、`Question`、`ExamRecord` 等实体类封装业务数据，DAO 层封装数据访问逻辑
- **View（JSP）**：负责页面展示，通过 EL 表达式和 JSTL 标签（或 Scriptlet）动态渲染
- **Controller（Servlet）**：接收请求、调用业务逻辑、决定转发/重定向路径

这种分离带来的好处是显而易见的：修改数据库查询逻辑只需改动 DAO 层，调整页面样式只需修改 JSP，业务流转变更只需调整 Servlet。在实际开发中，多人并行协作成为可能。

**2. Filter 和 Listener 的"横切关注点"威力**

- **Filter** 是处理"跨多个 Servlet 的通用逻辑"的理想方式。本系统中 `EncodingFilter` 只需一次配置即可为所有请求统一设置编码，避免了在每个 Servlet 中重复编写相同的编码代码。试想如果没有 Filter，项目中的 6 个 Servlet 都需要各自处理编码，不仅代码冗余，还容易遗漏。

- **Listener** 在"无侵入地观察和响应容器级别事件"方面展现了独特的价值。`UserSessionListener` 在 Session 创建/销毁时自动跟踪在线用户，无需在每个 Servlet 中手动管理在线状态。

**3. 从"面向过程"到"面向对象"的思维转变**

DAO 接口 + 实现模式让我深刻理解了面向接口编程的好处。`UserDao` 接口定义了统一的用户操作契约，`UserDaoImpl` 提供具体的 JDBC 实现。如果将来需要切换为 JPA 或 MyBatis，只需新增一个 `UserDaoJpaImpl` 实现类，而 Servlet 调用方完全无需修改。

### 4.2 对 JSP + Servlet + JavaBean 开发模式的理解

**1. 架构本质**

JSP + Servlet + JavaBean 是 Java Web 开发的经典组合，其本质是 **Model 2 架构**（即 MVC 模式在 Web 领域的实现）：

- **Servlet 作为控制器**：接收 HTTP 请求，解析参数，调用业务逻辑，路由到对应视图。Servlet 负责"流程控制"，不应包含业务逻辑和展示逻辑
- **JSP 作为视图**：专注于数据的呈现，尽量减少 Java 代码（Scriptlet），优先使用 EL 表达式和 JSTL 标签库
- **JavaBean 作为模型**：包含业务数据和业务规则，DAO 层负责数据持久化

**2. 核心优势**

- **职责分离清晰**：不同角色（页面美工、Java 开发、数据库管理员）可并行工作
- **组件可复用**：JavaBean 可以被多个 Servlet 和 JSP 复用
- **容器管理生命周期**：Servlet、Filter、Listener 的生命周期由 Web 容器管理，开发者专注业务逻辑
- **平台无关性**：基于 Java 标准，可部署在任何兼容 Servlet 规范的服务器上

**3. 局限与思考**

- **JSP 页面混杂 Java 代码**：本项目中大量使用 `<%= %>` Scriptlet 标签，导致页面可读性较差，后续可以使用 JSTL + EL 彻底替换
- **DAO 层代码重复**：每个 DAO 实现中都出现了类似的 `getConnection()` → `prepareStatement()` → `executeQuery()` → `close()` 模式，可以考虑引入模板方法或 ORM 框架简化
- **事务管理薄弱**：当前采用自动提交模式，在考试提交等涉及多表写入的场景中存在数据不一致的风险
- **前端技术落后**：相比现代的 Vue/React 前端框架，纯 JSP 后端渲染的交互体验和开发效率都有较大差距

### 4.3 总结

本次实验让我完整掌握了 JSP + Servlet + JavaBean 开发模式的完整流程：从需求分析、数据库设计、实体建模、DAO 编写，到 Servlet 路由控制、JSP 视图渲染，再到 Filter/Listener 的横切关注点处理。在线考试系统虽然规模不大，但涵盖了 Web 开发的典型要素——用户认证、权限控制、数据 CRUD、状态管理、会话跟踪，为我后续学习 Spring MVC、Spring Boot 等现代框架打下了扎实的基础。

最重要的收获是：理解了**分层架构的本质是为了管理复杂性**，这个思想在任何规模的软件项目中都同样适用。
